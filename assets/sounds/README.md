# Sounds Directory

Add alert sound files here:

- `alert_critical.wav` - Critical HP warning
- `alert_enemy.wav` - Enemy detected
- `alert_zone.wav` - Zone closing
- `startup.wav` - Jarvis startup chime
- `command_recognized.wav` - Command received beep

Use short (<1 second) WAV files for best performance.
